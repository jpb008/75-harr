// Project URL and anon (public) API key from your Supabase project —
// find them under Settings > API in the Supabase dashboard. The anon key
// is meant to be exposed client-side; access is enforced by Row Level
// Security policies on the `progress` table, not by keeping this secret.
//
// Leave both empty to run in guest-only mode (localStorage, no login).
export const SUPABASE_URL = ''
export const SUPABASE_ANON_KEY = ''
